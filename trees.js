//Tress

const prompt = require("prompt-sync")();


var no = prompt("How tall is my tree? "); //height of tree
var leaf = "#";//counter for leaves

for (let i = 0; i < no; i++){

    console.log(leaf);
    leaf += "#"
}
